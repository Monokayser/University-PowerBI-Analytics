# Screenshots

Power BI Desktop was not available in the build environment, so actual dashboard, model-view, Power Query, and Power BI Service screenshots were not captured.

Do not fabricate screenshots. After the `.pbix` is built in Power BI Desktop, place real screenshots in the relevant folders:

- `data_import/`
- `power_query/`
- `model_view/`
- `report_pages/`
- `powerbi_service/`
